@model BPCCScheduler.Models.Appointment

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Appointment</legend>

    <div class="display-label">ClientName</div>
    <div class="display-field">@Model.ClientName</div>

    <div class="display-label">Cell</div>
    <div class="display-field">@Model.Cell</div>

    <div class="display-label">Date</div>
    <div class="display-field">@String.Format("{0:g}", Model.Date)</div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}


